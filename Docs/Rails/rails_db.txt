rails db:migrate:redo STEP=3

// create the database, load the schema and initialize it with the seed data
rails db:setup

// drop the database and set it up again
// this is functionally equivalent to rails db:drop db:setup
rails db:reset

rails db:seed
