<%= form_for(:gossip, url: gossip_path, method: :patch) do |f| %>
  Auteur : <%= f.text_field :anonymous_author %><br />
  Contenu : <%= f.text_area :content %><br />
  <%= f.submit %>
<% end %>

<%= button_to 'Supprimer le gossip', @gossip, method: :delete, data: { confirm: "Are you sure?" } %>
<%= link_to 'Retour', root_path %>
